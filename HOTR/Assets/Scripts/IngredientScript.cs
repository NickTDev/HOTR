using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IngredientScript : MonoBehaviour
{
    public IngredientScriptableObject ingredientValues;

    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log(ingredientValues.ingredientName + " Ingredient Loaded");
        //
        //if (ingredientValues.isHeld)
        //    Debug.Log(ingredientValues.ingredientName + " is currently held");
    }
}
